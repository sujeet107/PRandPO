document.addEventListener('DOMContentLoaded', () => {
    // Example of future interactivity
    // This is where you might load data or add more interactive features

    console.log('Department 1 Dashboard loaded.');

    // Future: Add code to fetch real data and populate dashboard sections
});
